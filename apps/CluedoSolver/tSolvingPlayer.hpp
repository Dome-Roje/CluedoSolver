#pragma once

#include <tPlayer.hpp>

class tSolvingPlayer : public tPlayer {
    public:
        // tGame::tRumor startRumor() final;
        // tGame::tRumor makeAccusation() final;
        // tGame::eRumorResponce respondToRumor(tGame::tRumor& rumor) final;
        // void observeRumorResponce(tGame::tRumor& rumor, tPlayer& player, tGame::eRumorResponce& responce) final;
    protected:
    private:
};